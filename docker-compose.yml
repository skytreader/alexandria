version: '2'
services:
  #db:
  #  image: "mysql:5.7"
  #  environment:
  #    - MYSQL_ALLOW_EMPTY_PASSWORD=yes
  #  ports:
  #    - "3306:3306"
  web:
    build: .
    # command: python run.py
    command: python forever.py
    ports:
      - "80:7070"
    volumes:
      - .:/librarian
    #depends_on:
    #  - db
